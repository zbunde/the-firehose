require 'spec_helper'

describe "/" do
  #subject { page }
  #describe "home page should" do
  #  before { visit root_path }
  #  before { @user = User.new(name: "User", email: "user@example.com", provider: "github") }
  #
  #  it "should create a user" do
  #    expect { click_link "Login with Github"}.to change(User, :count).by(1)
  #  end
  #end
end
